{
  "type": "module",
  "dependencies": {
    "cheerio": "^1.0.0-rc.12",
    "markdown-escape": "^2.0.0",
    "moment": "^2.29.4",
    "node-fetch": "^2.6.11",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "esbuild": "0.21.5"
  },
  "scripts": {
    "build": "esbuild index.js --bundle --platform=node --outfile=build/index.js --ignore-annotations --legal-comments=none"
  }
}
